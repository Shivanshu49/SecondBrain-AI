# Leave empty in development: Vite proxies /api to the FastAPI server (see vite.config.js).
# For production builds served from a different origin, set your API base URL, e.g.:
# VITE_API_URL=https://your-api.example.com

VITE_API_URL=
